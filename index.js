/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {Attendance} from './src/Attendance';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Attendance);
